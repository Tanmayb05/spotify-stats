# Runtime dependencies for the FastAPI backend.
# Pinned to the versions in the project .venv (2026-09-01). Bump deliberately.

fastapi==0.119.0
uvicorn[standard]==0.37.0
pydantic==2.12.3   # 2.12.0 has a FieldInfo bug that breaks supabase-auth on Python 3.14
python-dotenv==1.1.1
pandas==2.3.3
numpy==2.3.3
scikit-learn==1.7.2

# app/ingest/validate.py runs this over every landed export batch (Phase 12).
# Runtime dep from this phase forward, not a dev nicety.
pandera==0.33.0

# Supabase/PostgREST backend (DB_BACKEND=supabase, the default).
# Imported lazily as of Phase 10, so the app boots without it configured; it is
# still required whenever the Supabase backend is selected.
# 2.31.0: 2.22.0 (the version that shipped in .venv) is yanked on PyPI and breaks
# under Python 3.14 / pydantic 2.12; 2.31.0 installs and imports cleanly on 3.13 + 3.14.
supabase==2.31.0

# Local Postgres backend (DB_BACKEND=local) + the migration runner.
sqlalchemy==2.0.52
psycopg[binary]==3.3.5
